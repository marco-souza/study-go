# Simple app running in a webview

## File Structure

## Dependencies

To build the code in your machine, you need `go get` the following packages:

- github.com/zserge/webview

Then just run `go build src/main.go`